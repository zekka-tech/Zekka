# Legacy Test Archive

This directory contains retired backend test artifacts that no longer belong in the active Jest suite.

- These tests are preserved for historical reference only.
- They target archived Session 2 security and legacy `auth-service` implementations.
- The active auth backend test surface is centered on `tests/unit/services/auth.service.test.js` and current integration coverage.
