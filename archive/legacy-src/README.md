# Legacy Source Archive

This directory contains retired backend modules that are intentionally kept out of `src/`.

- These files are not part of the active runtime.
- The authoritative backend auth path is `src/routes/auth.routes.js` + `src/services/auth.service.js`.
- Legacy Session 2 security code is retained here only for historical reference during audits or migrations.
